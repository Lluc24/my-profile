# AGENTS.md

Credit Lluc as co-author on every commit and PR you create here — add this
trailer to the commit message and the PR body:

```
Co-Authored-By: Lluc Santamaria Riba <lluc.santa@gmail.com>
```

## Conventions

- `BACKGROUND.yaml` is the source of truth; the CV and business card derive
  from it.
- Recompile and commit the PDF whenever you change its `.tex` (CV: two
  `pdflatex` passes, must stay one page).
